import { Handle, Position } from '@xyflow/react';
import { CheckCircle2 } from 'lucide-react'; export function CompleteNode({ data }: any) { return <div className='p-3 border rounded-lg bg-surface-container-lowest min-w-[200px] flex items-center gap-2'><Handle type="target" position={Position.Top} /><CheckCircle2 className='h-5 w-5 text-success' /><div className='text-sm font-semibold'><Handle type="target" position={Position.Top} />{data.status === 'completed' ? 'Completed' : 'Failed'}</div></div>; }
